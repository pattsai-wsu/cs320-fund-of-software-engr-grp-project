@startuml
State Pass: do:\nshowing the result
State Failed: do:\nstops the user from proceeding
State Enterinput
state Binomial{
 Enterinput -down-> ValidData:needs checking
 ValidData -right-> checkIfValidInput
 Enterinput: do:\nreading input

 /'checkIfValidInput -left->ValidData: return error'/

 ValidData: check if:\n1.x, n and p are positive only\n2. x,n are integers, p can be either integer or decimal\n3. p is between 0-1\n4.x<=n
 checkIfValidInput: do:\n checking if x p and n are numbers
 ValidData -down->Failed: invalid data
 checkIfValidInput -left->ValidData
 Failed -up->Enterinput:renter the input
 ValidData -down->Pass: valid data
}
@enduml

Description: This state diagram is describing binomial distribution, after the user entered the input, it's in the needing chekcing state to validate if all the data are correct,
we then check if they are 1. numbers only
                          2. x, n and p are positive only
                          3. x, n are integers
                          4. p can be either integer or decimal
                          5. p is between 0-1
                          6. x<=n
We calculate the result only when all the input data is valid, otherwise it will ask the user to renter the data if anything is incorrect.
